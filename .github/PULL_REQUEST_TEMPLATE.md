## Summary

<!-- What does this PR change? -->

## Test plan

- [ ] `cd backend && uvicorn app.main:app` — `/health` OK
- [ ] `cd frontend && npm run dev` — predict + bracket load
- [ ] No secrets committed (`.env`, API keys)
